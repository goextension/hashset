# hashset

no lisence,every body can do anything


```
    //create a hashset interface
    New() HashSet
    
    //add
    Add(i string) (b bool)
	  //check
    Has(i string) (b bool)
	  //remove
    Remove(i string)
    
```
